import React, {useState} from 'react'

import { AppBar, Toolbar, IconButton, Stack, Tab, Tabs} from '@mui/material';

const Sidebar = ({}) => {

    return (
        // {STACK} IS NEEDED FOR COLUMN EVEN SPACING OF PAGE ICONS
        // {TABS} can be used to order Buttons vertically, check the docs
        <nav> Sidebar </nav>
    );
}


export default Sidebar;